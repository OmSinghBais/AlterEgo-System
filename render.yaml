services:
  - type: web
    name: alterego-backend
    env: docker
    plan: free
    rootDir: voice-system/backend
    
    dockerfile_path: Dockerfile
    docker_context: .

    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: MODEL_NAME
        value: gpt-4o
